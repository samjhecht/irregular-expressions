# Anthropic API Configuration
# Required: Your Anthropic API key for Claude access
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Override the default Claude model (default: claude-3-haiku-20240307)
ANTHROPIC_MODEL=claude-3-haiku-20240307

# Optional: Maximum prompt length in characters (default: 4000)
MAX_PROMPT_LENGTH=4000

# Optional: Maximum tokens for Claude responses (default: 1000)
MAX_TOKENS=1000

# Database Configuration (if using database features)
RENDER_DATABASE_URL=your_database_url_here